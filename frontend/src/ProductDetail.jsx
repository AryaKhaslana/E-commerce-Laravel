export default function ProductDetail() {
    return (
        <h1>ini halaman detail</h1>
        // 1. Ambil ID dari URL (pake useParams)
        // 2. Pas halaman dibuka, panggil API /api/products/{id}
        // 3. Simpan data di state
        // 4. Kalau loading, tampilin spinner
        // 5. Kalau ada data, tampilin Foto, Nama, Deskripsi
    );
}